import React from "react";

const Dropdown = () => {
    return(
    <div>
        <select value={"Price high to low"}>
            <option>Price high to low</option>
            <option>Price low to high</option>
        </select>
    </div>
    );
};

export default Dropdown;

